apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "odoo-openapi.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "odoo-openapi.labels" . | nindent 4 }}
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "odoo-openapi.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "odoo-openapi.selectorLabels" . | nindent 8 }}
    spec:
      {{- if .Values.image.pullSecret }}
      imagePullSecrets:
        - name: {{ .Values.image.pullSecret }}
      {{- end }}
      containers:
        - name: {{ .Chart.Name }}
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
            readOnlyRootFilesystem: true
            allowPrivilegeEscalation: false
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.api.port }}
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
          resources:
            limits:
              cpu: 1000m
              memory: 1Gi
            requests:
              cpu: 100m
              memory: 256Mi
          env:
            - name: API_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ include "odoo-openapi.fullname" . }}-api-secret
                  key: api-key
            - name: HOST
              value: "0.0.0.0"
            - name: PORT
              value: {{ .Values.api.port | quote }}
            - name: ODOO_URL
              value: {{ .Values.odoo.url | quote }}
            - name: ODOO_DB
              value: {{ .Values.odoo.db | quote }}
            - name: ODOO_USER
              value: {{ .Values.odoo.user | quote }}
            - name: ODOO_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "odoo-openapi.fullname" . }}-odoo-secret
                  key: password
            - name: PYTHONUNBUFFERED
              value: "1"
            - name: PYTHONPATH
              value: "/app"
            {{- range $env := .Values.env }}
            - name: {{ $env.name }}
              value: {{ $env.value | quote }}
            {{- end }}
