apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
  namespace: {{ .Release.Namespace }}
  labels:
    app: pgpool
    release: {{ .Release.Name }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: pgpool
    release: {{ .Release.Name }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: 5432
    protocol: TCP
    name: postgresql
