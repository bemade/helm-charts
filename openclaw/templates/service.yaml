apiVersion: v1
kind: Service
metadata:
  name: openclaw
  namespace: {{ .Release.Namespace }}
  labels:
    app: openclaw
spec:
  selector:
    app: openclaw
  ports:
    - protocol: TCP
      port: {{ .Values.openclaw.port }}
      targetPort: {{ .Values.openclaw.port }}
      name: http
