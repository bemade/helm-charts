apiVersion: v1
kind: ConfigMap
metadata:
  name: openclaw-config
  namespace: {{ .Release.Namespace }}
  labels:
    app: openclaw
data:
  openclaw.json: |
    {{- .Values.openclaw.config | toPrettyJson | nindent 4 }}
