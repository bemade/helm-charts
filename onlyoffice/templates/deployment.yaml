apiVersion: apps/v1
kind: Deployment
metadata:
  name: onlyoffice
  namespace: {{ .Release.Namespace }}
  labels:
    app: onlyoffice
spec:
  replicas: 1
  selector:
    matchLabels:
      app: onlyoffice
  template:
    metadata:
      labels:
        app: onlyoffice
    spec:
      volumes:
        - name: ssl-cert
          secret:
            secretName: onlyoffice-cert
      containers:
        - name: onlyoffice
          image: onlyoffice/documentserver
          resources:
            requests:
              cpu: 2
              memory: 4Gi
          ports:
            - containerPort: 443
          env:
            - name: JWT_ENABLED
              value: "true"
            - name: JWT_HEADER
              value: Authorization
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: jwtsecret
                  key: jwtsecret
          volumeMounts:
            - name: ssl-cert
              mountPath: /var/www/onlyoffice/Data/certs/onlyoffice.crt
              subPath: tls.crt
              readOnly: true
            - name: ssl-cert
              mountPath: /var/www/onlyoffice/Data/certs/onlyoffice.key
              subPath: tls.key
              readOnly: true
